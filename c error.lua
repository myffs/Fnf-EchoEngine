-- this is not a real lua, i don't recommend using it, unless you want there to be an error called ''c''
function onCreatePost()
    create text()
        'callate estupido :clown:
                function noteMissPress(direction)
            -- Called after the note press miss calculations
            -- Player pressed a button, but there was no note to                 hit (ghost miss)
        end
    end
end
